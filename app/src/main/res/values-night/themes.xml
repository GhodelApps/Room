<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Room" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bgnight</item>
        <item name="colorPrimaryVariant">@color/actionnight</item>
        <item name="colorOnPrimary">@color/action2night</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/maintextnight</item>
        <item name="colorOnSecondary">@color/text2night</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
    </style>
</resources>